DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=microcms
DB_USER=root
DB_PASS=local_admin

ADMIN_USER=admin
ADMIN_PASS=changeme

# Leave empty when microCMS is inside the site root.
# Use ../website only when developing as a sibling of the website folder.
SITE_ROOT=
